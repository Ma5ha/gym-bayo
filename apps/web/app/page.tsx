

export default async function Index() {
  /*
   * Replace the elements below with your own.
   *
   * Note: The corresponding styles are in the ./index.scss file.
   */
  return (
   
    <button className="text-3xl font-bold underline">ola
      change 
    
    </button>
  );
}
